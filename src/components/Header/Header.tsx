import React from 'react';
import './Header.css';
import Toolbar from '../../UI/Toolbar/Toolbar';

const Header = () => {
  return (
    <header>
      <Toolbar />
    </header>
  );
};

export default Header;
